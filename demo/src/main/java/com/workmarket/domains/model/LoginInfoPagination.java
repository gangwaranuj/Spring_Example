package com.workmarket.domains.model;

public class LoginInfoPagination extends AbstractPagination<LoginInfo> implements Pagination<LoginInfo> {

	public enum FILTER_KEYS {
		INTERNAL_USERS_FLAG
	}
	public enum SORTS {}
	
}
