package com.workmarket.search.request.work;

/**
 * Enumeration used to track the various search types against the work core
 */
public enum WorkSearchType {
    UNKNOWN,
    GLOBAL,
    DASHBOARD,
    HOMEPAGE_SYSTEM;
}
