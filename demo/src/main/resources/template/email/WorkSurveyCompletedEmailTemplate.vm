			<h1 style="font-family: Helvetica Neue, Arial, sans-serif; font-weight: 700; font-size: 20px; margin: 0px 0px 10px 0px; color: #fe8707; padding: 0px 20px 10px 20px; border-bottom: 1px solid #eee;">Survey Completed</h1>

			<div style="width: 520px; padding: 20px 20px 20px 20px; font-family: Helvetica Neue, Arial, sans-serif; color: #444; font-size: 13px; line-height: 18px;">

			<p style="margin: 0px 0px 20px 0px">
				Hello ${toUser.firstName},<br/>
				<br/>
				${template.attempt.assessmentUserAssociation.user.fullName} has completed the survey as part of your assignment, <strong>${template.attempt.workTitle}</strong> <small>(${template.attempt.work.workNumber})</small>. The details are below.<br/>
				<br/>
				Survey: ${template.attempt.assessmentUserAssociation.assessment.name}<br/>
				Worker Name:
				<a href="${baseurl}/profile/${template.attempt.assessmentUserAssociation.user.userNumber}">${template.attempt.assessmentUserAssociation.user.fullName}</a>
			</p>

			<p>
			<a href="${baseurl}/assignments/details/${template.attempt.work.workNumber}">View this assignment</a>
			</p>
            #parse("template/email/partials/buyer-footer.vm")
		</div>
	</td>
</tr>

#parse("template/email/partials/do-not-reply.vm")

#if (${template.encryptedWorkFollowId})
	#parse("template/email/partials/unfollow.vm")
#end